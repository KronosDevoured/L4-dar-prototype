[Locations]
# Path to loadout config. Can use relative path from here.
looks_config = ./appearance.cfg

# Path to python file. Can use relative path from here.
python_file = ./tornado_spin_measurement_bot.py

# Name of the bot in-game
name = Tornado Spin Measurement

[Details]
# These values are optional but useful metadata for your bot.
description = Measures tornado spin radii at different stick angles and magnitudes
fun_fact = Tests 5 angles × 2 magnitudes to map complete tornado spin behavior
github =
language = python
tags = measurement, physics, tornado, dar
